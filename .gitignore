# --- Python Artifacts ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environments
venv/
env/
ENV/
.env
.venv

# --- Terraform Artifacts (Critical for IaC Security) ---
# Local .terraform directories
**/.terraform/*

# .tfstate files (These contain sensitive data like passwords!)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
# such as password, private keys, etc.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually local to the environment
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# --- OS Junk ---
.DS_Store
Thumbs.db
